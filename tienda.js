"use strict";const $=id=>document.getElementById(id);document.addEventListener("DOMContentLoaded",async()=>{const slug=new URLSearchParams(location.search).get("slug");if(!slug){$("storeStatus").textContent="Tienda no especificada.";return}try{const t=await TECNICOM_API.getTienda(slug);if(!t)throw new Error();$("storeStatus").hidden=true;$("storeName").textContent=t.nombre||"Tienda";$("storeDescription").textContent=t.descripcion||"";const info=$("storeInfo");info.hidden=false;info.textContent=[t.direccion,t.telefono,t.horario].filter(Boolean).join(" · ");const productos=Array.isArray(t.productos)?t.productos:[];const g=$("storeProducts");productos.forEach(p=>{const a=document.createElement("a");a.className="card";a.href=`producto.html?slug=${encodeURIComponent(p.slug||"")}`;a.style.textDecoration="none";a.innerHTML=`<div class="card-body"><h3>${p.nombre||""}</h3><p class="price">${precio(p.precio)}</p></div>`;g.appendChild(a)})}catch(e){$("storeStatus").textContent="No fue posible cargar esta tienda."}});function precio(x){const n=Number(x);return Number.isFinite(n)?new Intl.NumberFormat("es-CL",{style:"currency",currency:"CLP",maximumFractionDigits:0}).format(n):""}