"use strict";const $=id=>document.getElementById(id);document.addEventListener("DOMContentLoaded",async()=>{const slug=new URLSearchParams(location.search).get("slug");if(!slug){$("productStatus").textContent="Producto no especificado.";return}try{const p=await TECNICOM_API.getProductoMercado(slug);if(!p)throw new Error();$("productStatus").hidden=true;$("productDetail").hidden=false;$("productName").textContent=p.nombre||"";$("productCategory").textContent=p.categoria||"";$("productPrice").textContent=precio(p.precio);$("productDescription").textContent=p.descripcion||"";$("productSeller").textContent=p.negocio_nombre?`Vendido por ${p.negocio_nombre}`:"";if(p.imagen){$("productImage").src=media(p.imagen);$("productImage").alt=p.nombre||"Producto"}if(p.negocio_slug)$("productStore").href=`tienda.html?slug=${encodeURIComponent(p.negocio_slug)}`;else $("productStore").hidden=true}catch(e){$("productStatus").textContent="No fue posible cargar este producto."}});function media(x){return /^https?:/.test(x)?x:`https://www.carnesdiaz.cl/${String(x).replace(/^\/+/,"")}`}function precio(x){const n=Number(x);return Number.isFinite(n)?new Intl.NumberFormat("es-CL",{style:"currency",currency:"CLP",maximumFractionDigits:0}).format(n):""}